import { AppBlockEventDirective } from './app-block-event.directive';

describe('AppBlockEventDirective', () => {
  it('should create an instance', () => {
    const directive = new AppBlockEventDirective();
    expect(directive).toBeTruthy();
  });
});
